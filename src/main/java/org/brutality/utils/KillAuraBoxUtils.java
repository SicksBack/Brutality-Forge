package org.brutality.utils;

import net.minecraft.entity.Entity;
import java.awt.Color;

public class KillAuraBoxUtils {
    public static void drawTargetBox(Entity entity, Color color, float partialTicks) {
        // Your implementation to draw a box around the entity with the given color
        // Example (pseudo-code):
        // RenderUtils.drawBoundingBox(entity.getPosition(), color, partialTicks);
    }
}
